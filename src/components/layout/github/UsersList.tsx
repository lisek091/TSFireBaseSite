/*
import { UsersType } from "../../context/redux/actions/GithubActionsTypes";



const UsersList = (users: UsersType) => {
    if (users === undefined) {
        return <div>Loading...</div>
    }
    else {

        return <div>{users.map((user) => {
            <div>
                {user.login}
            </div>
        })}</div>;
    }
};

export default UsersList;
*/
import React from 'react';

const UsersList = () => {
    return <div></div>;
};

export default UsersList;
